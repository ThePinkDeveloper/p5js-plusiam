const BASE_WIDTH = 412;
const BASE_HEIGHT = 780;

function setup() {
    var canvas = createCanvas(BASE_WIDTH, BASE_HEIGHT);
    canvas.parent('main');
    background(255, 0, 200);   
}

function draw() {

}


